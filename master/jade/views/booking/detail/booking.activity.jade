div(style="margin: -8px;")
  md-card(md-theme="menucloud-std")
    md-card-title
      md-card-title-text(flex="90" flex-xs="80")      
        legend.col-sm-12(style='margin-bottom: 10px;') ACTIVITY FEED

    div(style="width: 75%; margin: auto; padding: 30px 0;")
      booking-activity-item(
          data-ng-repeat="item in vm.activities"
          data-activity="item"
      )
